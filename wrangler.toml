name = "cohrint"
compatibility_date = "2024-01-01"
pages_build_output_dir = "./cohrint-frontend"

# ── Environment variables (non-secret, commit-safe)
[vars]
ENVIRONMENT = "production"

# ── Preview environment
[env.preview.vars]
ENVIRONMENT = "preview"
